2026.3.23 电子信息与通信学院 郝锦龙 在上午12点接到了工程题，早就听说会有惊喜，打开一看还真是 任务书里说的也很清楚了有三个独立任务，大致浏览下来发现都和仓颉程序有关，这个我还不太了解，于是跑去问了ai，说是华为自己开发的的一个编程语言，不过好在任务书后面也提到了编程语言不限，那必然是用我们最熟悉的C语言来进行开发了，那么接着就着手第一个任务。 第一个任务大致思路就是实现把.SVG文件解析出来并渲染成我们能直接看的图片格式，我这里就选.BMP格式较为简单，那么.svg文件到底是什么，去查了资料，发现是一种基于.XML的文本格式，是由数学路径来描述一个图像，简单说就是告诉我们图像里的一些曲线啥的是怎么画出来的并给出其的一些参数。 那么我们要做的相当于是先进行XML解析，用程序去读.svg，然后把里面的数字内容提取出来，同时我了解到.svg文件里的一些常见的需要处理的元素例如 路径的d属性 样式：fill（颜色） stroke（颜色/宽度） 然后就是，任务书提到是我们用命令行去读取.svg文件，防止使用固定数据作弊，于是要在main函数里进行传参 读文件要注意的是任务书里提到图像短板尺寸不能少于1000px且不能改变原图的宽高比 我们先得提取出.SVG的宽高，我点开.SVG看了一下然后问了ai大致怎么提取信息，要先读viewBox和尺寸还有一大堆的细节要实现，这部分我花费了起码两个小时让ai生成了一个理论上可以直接运行的，但是很遗憾，ai生成的函数根本无法解析复杂的曲线，于是打算亲自写 接下来我花费了一个下午研究上述的.svg文件大致得出以下信息 有用的东西 width="1280.000000pt" height="1248.000000pt" viewBox="0 0 1280.000000 1248.000000" 类似这种是画布的尺寸和范围 <g transform="translate(320.000000,900.000000) scale(0.0500000,-0.0500000)" 这一块是坐标变换 fill="#000000" stroke="none"> 这个是填充颜色和描边颜色 <path d= 有的测试集里有一大堆的这种东西，经查询发现是描述图形轮廓的标签，d属性是路径指令，还有些常见的指令含义如M（移动），L（画直线），C（贝塞尔曲线），Z（回到起点） 小写字母（相对坐标） (多边形） points="166,709.5 189.5,696 163.5,657.5 这种是多边形的顶点坐标 据我观察大致只有上述这些指令 3.25 由于昨天早上晚上都有大物实验，所以基本没怎么继续做，今天早上在搜集了大量资料后发现了github上有现场的头文件可以用于解析渲染svg文件 于是便将它们下载了下来，具体头文件： https://github.com/memononen/nanosvg/blob/master/src/nanosvg.h https://github.com/memononen/nanosvg/blob/master/src/nanosvgrast.h https://github.com/nothings/stb/blob/master/stb_image_write.h 那有了这三个头文件第一个任务那就轻而易举了，直接引入头文件并且解析尺寸渲染即可，该过程只花费了半个小时结束，最后经测试，将提供的.svg文件加入.exe文件的目录中，在命令行读取提供的6个.svg，均能渲染并生成合格的.png文件 那么由此第一个任务就算完工了
